# Change this single value before a production frontend build.
# The value is embedded by Vite at build time: rebuild and redeploy after changing it.
VITE_API_BASE_URL=https://api.locationnovavolt.ca/api/v1